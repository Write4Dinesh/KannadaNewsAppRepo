<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sgdm.KannadaNewsApp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".model.KNAApplication"
        android:allowBackup="true"
        android:icon="@mipmap/kannada_news"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/kannada_news"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ui.KNASplashActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.KNAHomeActivity" />
        <activity android:name=".ui.KNABrowserActivity" />
        <activity android:name=".ui.KNASuperActivity"/>
    </application>

</manifest>